---
name: frontend-app-architecture-quality-review
description: This skill should be used when performing comprehensive architecture quality reviews for front-end applications. It provides systematic workflows for analyzing build & dependency management, base framework, utilities, business modules, and third-party integrations. The actual tech stack should be derived from the current project.
version: 0.0.2
---

# 前端应用架构质量审查计划（Skill）

## 技能概述

本技能指导 AI 助手对“前端应用”进行系统性的架构质量审查。审查应以当前项目事实为准（代码、依赖、构建配置、运行形态），并将结论输出到仓库的 `.plans/` 目录中。

## 使用时机

**重要说明：** 本技能专门用于项目级架构质量审查，关注整体架构设计、模块划分、依赖关系等架构层面的问题。

在以下情况下应该使用此技能：

**核心使用场景 - 架构审查：**

- 需要对整个前端应用进行架构质量审查
- 评估项目架构设计的合理性和可维护性
- 识别架构层面的设计问题和改进机会
- 评估技术选型和架构决策的合理性
- 项目重构前的架构评估

## 审查维度（通用）

本技能从前端应用的五个核心构成部分出发，对架构质量进行系统审查：Build（构建与依赖管理）、基础框架（Base Framework）、Utils（工具层）、业务组件/模块、第三方库（集成与隔离）。

说明：本 Skill 的“前端应用”是对交互型客户端的抽象，既包括 Web（浏览器）、也包括各类客户端运行形态（如 iOS/Android、Hybrid/小程序等）。不同技术栈不改变审查结构，均按同一套五个 Phase 进行 review；`references/` 仅提供不同栈的落地口径样例。

### Phase 1 Build（构建与依赖管理）

#### 1.1 审查目标

- 确保构建工具链与打包配置（如 Webpack/Vite/Rspack/Rollup/Next.js/自研脚手架等）结构清晰、职责单一、便于维护和扩展。
- 控制依赖版本与冲突，避免不必要的冗余依赖和隐性升级风险。
- 清晰区分不同环境（开发 / 测试 / 生产）的构建配置，降低配置错误风险。
- 优化构建性能，缩短构建时间，提升开发与 CI 效率。

#### 1.2 核心关注点（待定）

#### 1.3 其它关注点

- **模块与依赖关系**：
  - 项目模块划分是否合理（src/、view/、components/、api/、libs/、directive/等）
  - 是否存在循环依赖或不合理的依赖关系
  - 基础模块是否被高层模块错误依赖

- **构建配置规范性**：
  - 构建配置文件（如 `webpack.config.*` / `vite.config.*` / `next.config.*` 等）是否清晰合理
  - 是否使用统一的依赖与版本管理入口（如 Web 的 `package.json`，Android 的 Gradle 版本集中治理，iOS 的 Podfile/SPM 配置等）
  - 是否有清晰的注释说明关键配置
  - 构建配置与依赖解析是否可复现且可审计（锁定策略、配置集中治理、CI 环境一致性）
  - 环境/配置 profile 管理是否规范（如 Web 的 `.env.*`，Android 的 `buildTypes`/`productFlavors`，iOS 的 `xcconfig`/Scheme/Build Settings 等），并避免来源不明的“多源覆盖”
  - 是否存在构建阶段副作用（例如构建/启动时写入运行时配置文件），是否会导致本地与 CI 环境不一致
  - 是否正确配置本地开发代理（如 devServer proxy），避免跨域与环境切换问题被业务代码“硬编码”掩盖

- **版本与仓库管理**：
  - 依赖版本是否固定且可追踪（如 Web 的 lockfile，Android 的 Gradle 依赖锁定/版本集中定义，iOS 的 `Podfile.lock` / `Package.resolved` 等）
  - 是否避免使用不可信仓库
  - 是否定期评估和升级关键依赖
  - 构建期常量/环境注入机制是否统一（如 .env、构建时注入、运行时配置下发等），避免多套并存

- **构建类型与环境**：
  - 是否正确配置开发、测试、生产环境
  - 多环境/多渠道/多 Scheme（若存在）的配置边界是否清晰，避免环境漂移与配置爆炸
  - 环境变量是否通过安全方式注入
  - 是否遵循所用构建系统的配置/环境注入约定（如 Web 的环境变量前缀约定，移动端的 profile/构建配置注入机制），避免来源不明的“隐式注入”
  - 环境信息是否存在“双源/多源”导致的环境漂移风险（例如请求 baseURL 与路由 dev 判断来自不同环境源）
  - 开发态/生产态功能开关是否完全由环境源控制，避免出现“生产误暴露 Demo/脚手架页面”的风险
 
- **安全与合规**：
  - 构建配置中是否存在明文秘钥、账号密码或敏感 URL
  - 生产构建产物是否移除或隔离调试能力（debug 页面、测试路由、Mock、source map 暴露策略等）
  - 依赖与构建产物是否满足供应链与合规要求（来源可信、版本可追踪、许可证风险可评估）
  - 是否存在不利于审计与静态分析的构建做法（如动态执行配置、危险的字符串拼装注入等）
  - CI/CD 中的构建权限与密钥注入是否最小化、可审计（环境隔离、最小权限、避免日志泄漏）

#### 1.4 审查产物输出确认

- **整体审查报告（必须覆盖全部内容）**：
  - 必须覆盖本 Phase 全量其它关注点
  - 不能仅做条目汇总；必须给出跨主题的全局整合视角（构建配置治理、环境一致性、依赖与供应链风险、治理优先级、落地路线）
  - 整体审查报告需在收敛阶段补齐跨主题整合结论。
  - **整体审查报告**：`/.plans/架构质量审查-001-Build.md`

### Phase 2 基础框架（Base Framework）

#### 2.1 审查目标

- 确保应用整体架构模式（如 MVC/MVVM/组件化架构）在各模块中得到一致、正确的实现。
- 建立清晰的分层与边界（视图层 / 业务逻辑层 / 数据层），避免横向耦合和"上帝组件"。
- 通过统一的基础组件（如布局组件、路由配置、状态管理、API 封装）提升复用性与可维护性。
- 提供稳定的错误处理、日志与配置体系，为排查问题和运维提供支撑。
- 明确主报告的全局整合作用：主报告需覆盖本 Phase 全部关注点，并对专项审查结果进行跨主题整合（依赖边界、系统性风险、治理优先级与落地路线）。

#### 2.2 核心关注点

- **UI 框架与设计系统**：
  - 项目主体使用的 UI 框架/组件库是什么（自研/开源/商业），是否存在多 UI 框架混用
  - 是否存在二次封装层（Design System / UI Adapter），以及与业务组件的依赖方向是否清晰
  - 样式体系是否统一（主题、暗色模式、CSS 方案、全局变量、覆盖策略），是否存在样式污染与冲突
  - **审查报告**：`/.plans/架构质量审查-002-基础框架-UI.md`

- **骨架屏**：
  - 语义边界是否清晰：Skeleton（结构占位）vs Loading Screen（整页加载态）vs Loading Bar/Spinner（进度/忙碌指示），是否按场景选择而非“一刀切”
  - “首屏/路由切换/局部模块”三类加载态是否分层治理：页面级（route-level）与模块级（component-level）是否可独立控制与组合
  - 骨架屏是否与最终 UI 结构一致（尺寸、行高、间距、网格/卡片骨架），避免布局跳动（CLS）与渲染抖动
  - 数据与资源加载策略是否匹配：首屏关键数据并行、图片/字体/大组件懒加载时是否有占位与渐进展示策略
  - 交互与可访问性：加载态是否阻断/允许用户操作有明确策略；是否提供可读的无障碍提示（aria-busy/aria-live 等）且避免“永远转圈”
  - 超时与失败兜底是否明确：加载超过阈值是否切换到可恢复的错误/空态（重试、刷新、回退），并避免骨架屏长期停留掩盖错误
  - 资源与性能：骨架屏实现是否轻量（避免引入重型动画/高频重绘）；是否支持按需加载与 SSR/CSR 一致性（避免 hydration mismatch）
  - 可观测性：是否统计首屏渲染/路由切换耗时、骨架屏曝光时长与失败率，作为性能与稳定性治理指标
  - **审查报告**：`/.plans/架构质量审查-002-基础框架-骨架屏.md`

- **弹窗**：
  - 范围是否明确：仅覆盖全局弹窗/通知/公告/Modal；是否明确不将 toast 等区域通知纳入本关注点（避免口径混乱）
  - 统一入口是否存在：是否有集中式的 Modal Service / Dialog Manager（或等价机制），避免页面/组件内散落实现与多套弹窗组件并存
  - 依赖方向是否正确：业务模块是否仅依赖“弹窗抽象接口/协议”，UI 实现是否可替换；是否避免业务层直接 import 具体弹窗组件
  - 使用方式是否可治理：是否支持声明式（基于配置渲染）与命令式（open/close）两种方式的边界；是否有统一的关闭策略（用户点关闭/确认、超时自动关闭、路由切换关闭、登出关闭）
  - 并发与队列策略是否明确：多弹窗同时触发时的仲裁规则（优先级、互斥/可叠加、队列/合并、频控），避免“弹窗风暴”与遮挡关键操作
  - 与路由/页面生命周期的关系是否清晰：是否区分页面级弹窗与全局弹窗；路由切换/多 Tab/多窗口（若适用）时弹窗归属与销毁策略是否明确
  - 接口数据结构与配置化：弹窗/公告是否来自后端配置；Schema 是否版本化、可校验（必填字段、类型、默认值、兼容策略）；是否支持灰度/人群/频控/有效期/展示条件等配置
  - 安全与风控：配置来源是否可信（签名/白名单/后端校验）；跳转协议（deeplink/url）是否校验与降级；是否防止通过配置注入恶意链接/路由
  - 可观测性闭环：曝光/点击/关闭原因/转化是否可追踪；弹窗触发链路是否可定位（触发源、决策规则命中、最终展示原因）
  - 用户体验：是否避免强制弹窗阻断关键流程；强制弹窗是否具备合理时机与可恢复路径；公告阅读与确认是否可审计（若合规需要）
  - **审查报告**：`/.plans/架构质量审查-002-基础框架-弹窗.md`

- **登录与账号体系**：
  - 登录入口与鉴权链路是否清晰（登录态获取、续期/刷新、登出、失效处理）
  - Token/Session 的存储策略是否安全（Cookie/LocalStorage/Memory），是否有统一的请求拦截与错误处理
  - 用户信息与权限模型的来源与缓存策略是否明确（角色/权限点/路由权限），是否有一致的访问控制边界
  - 权限数据缺失/获取失败时的默认策略是否安全（默认拒绝 vs 默认放行）
  - 不同环境下鉴权策略是否一致（例如 token 注入、header 策略、cookie 策略）
  - 动态路由/菜单是否具备可信来源校验（白名单/签名/后端校验），避免本地存储被篡改造成路由注入
  - **审查报告**：`/.plans/架构质量审查-002-基础框架-登录.md`

- **检查更新与版本治理**：
  - 更新策略是否明确：可选更新 vs 强制更新，是否支持灰度/分渠道/分地区/分设备策略
  - 阻断时机是否合理：强制更新是否在进入关键业务流程前拦截（而非进入主页面后才提示）
  - 频控与用户体验：关闭/稍后再提醒的口径是否清晰、实现是否正确（时间差计算、跨版本策略）
  - 下载与安装链路是否可控：商店更新 vs 直链下载，是否具备失败重试、断点续传、仅 Wi-Fi 等策略
  - 安全与合规：下载 URL 白名单/HTTPS、APK 完整性校验（hash/signature）、未知来源安装授权引导
  - 可观测性闭环：曝光/点击/下载成功率/安装成功率/失败原因是否可追踪与告警
  - **审查报告**：`/.plans/架构质量审查-002-基础框架-检查更新.md`

#### 2.3 其它关注点

- **架构模式一致性**：
  - 是否在全局范围内明确并坚持某一种架构模式
  - 是否存在同一模块内混用多种模式的情况
  - 是否正确使用当前项目的主框架范式（如组合式 API、声明式组件模型、依赖注入、响应式/数据绑定等）

- **分层与依赖方向**：
  - 上层是否只依赖下层抽象接口而非具体实现
  - 是否存在视图层直接操作数据源或 API 层的情况
  - 是否正确使用当前项目的状态管理方案（如全局 Store、模块化状态域、事件/消息驱动状态等）
  - 是否正确使用当前项目的路由/导航方案（如声明式路由、集中式导航服务、导航守卫/拦截机制等）

- **基础组件设计**：
  - 布局组件（Layout、Header、Sidebar、Footer）是否只承担通用职责
  - 是否被滥用为"万能基类"，导致子组件难以扩展或重用
  - 是否正确使用当前项目框架提供的跨层通信/依赖注入机制（如 provide/inject、context 等）

- **运行时边界与生命周期治理**：
  - 资源创建/释放、前后台切换、路由栈/页面栈变化等是否有统一治理，避免泄漏与重复初始化
  - 线程/调度模型是否清晰（UI 线程/主线程约束、任务取消、并发边界），避免副作用失控

- **网络与数据访问框架**：
  - 是否存在统一的 API 封装（如统一 HTTP Client、拦截链/中间件机制）
  - 错误与异常是否集中处理
  - 重试与超时策略是否可配置
  - 非登录场景下的请求治理（幂等、熔断、降级）是否清晰

- **日志与错误收集**：
  - 是否有统一的日志工具与错误收集框架
  - 日志级别与输出在不同构建环境中是否区分管理
  - 日志输出是否避免打印敏感信息
  - 是否支持按模块或功能维度配置日志开关
  - 是否具备可观测性闭环（路由、请求、关键用户动作能够被关联追踪）

- **配置与开关**：
  - 是否存在统一的配置中心（如常量、开关、实验配置）
  - 避免散落的硬编码和魔法值
  - 是否正确使用环境变量管理配置
  - Feature Flag/实验开关的来源、默认策略与生效链路是否可审计、可回滚

#### 2.4 审查产物输出确认

- **核心关注点审查报告**：
  - `/.plans/架构质量审查-002-基础框架-UI.md`
  - `/.plans/架构质量审查-002-基础框架-骨架屏.md`
  - `/.plans/架构质量审查-002-基础框架-弹窗.md`
  - `/.plans/架构质量审查-002-基础框架-登录.md`
  - `/.plans/架构质量审查-002-基础框架-检查更新.md`

- **整体审查报告（必须覆盖全部内容）**：
  - 必须覆盖本 Phase 全量核心关注点 + 其它关注点
  - 不能仅做条目汇总；必须给出跨主题的全局整合视角（分层一致性、依赖边界、风险优先级、治理路线图）
  - 需要明确各专项之间的耦合关系与系统性风险（例如登录链路与路由权限、更新策略与可观测性）
  - 整体审查报告需在收敛阶段补齐跨主题整合结论。
  - **整体审查报告**：`/.plans/架构质量审查-002-基础框架.md`

### Phase 3 Utils（工具层）

#### 3.1 审查目标

- 将通用能力从业务中抽离为工具层，提升代码复用率，减少重复实现。
- 控制工具类的职责范围，避免形成难以测试和理解的"工具黑洞"。
- 确保工具层对框架运行时（响应式、生命周期、全局单例等）的依赖是清晰、可控且不会引入内存泄漏。

#### 3.2 核心关注点

- **本地存储与缓存（Storage）**：
  - 存储分层是否清晰：KV（配置/状态）vs 文件缓存（可过期）vs DB（结构化/查询）
  - 是否避免在工具层散落多套存储实现（多套 SP/MMKV/DB/文件缓存并存）且缺少治理规范
  - Key/Schema 是否集中治理：命名规范、命名空间/模块隔离、迁移策略（升级/灰度/回滚）
  - 数据生命周期是否可控：登录/登出、切换账号、撤回同意、风控触发等场景下的清理策略是否明确
  - 安全与合规：敏感数据是否最小化落盘；必须落盘时是否加密；备份/导出/Root 风险是否评估
  - 性能与可靠性：主线程写入控制（commit/apply）、高频写入合并、容量上限/淘汰策略、读写失败兜底
  - 可观测性：命中率、占用、清理耗时、写入失败率等指标是否可监控
  - **审查报告**：`/.plans/架构质量审查-003-Utils-Storage.md`

- **网络与请求（Http）**：
  - 是否有统一的请求客户端与分层封装（base client / service / api modules），避免页面直调
  - 请求入参与响应的类型/Schema 是否有约束（TS 类型、运行时校验、转换层），避免隐式字段漂移
  - 是否有统一的超时、重试、取消（AbortController）、并发控制与防抖/合并策略
  - 错误处理是否标准化（错误码映射、业务错误 vs 系统错误、用户提示口径）
  - 是否支持可观测性（requestId、链路追踪、耗时统计、失败原因分桶）
  - 安全与合规（敏感 header/参数脱敏、证书/HTTPS、token 注入边界）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-Http.md`

- **日志（Log）**：
  - 日志分级与环境策略是否清晰（dev/test/prod 的输出与采样）
  - 是否有统一的日志 API 与结构化字段（模块、事件名、traceId/requestId、用户态）
  - 是否避免敏感信息泄露（脱敏、黑名单字段、PII 最小化）
  - 是否支持持久化/上传与失败兜底（离线缓存、重试、限流）
  - 是否与错误收集/性能指标联动形成闭环（异常、关键动作、请求）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-Log.md`

- **加解密**：
  - 算法与实现是否统一入口（加密/解密/签名/验签），避免散落多套实现
  - Key/IV/盐值等敏感材料的管理是否合规（不硬编码、生命周期、环境隔离）
  - 编码与格式是否统一（base64/hex、padding、字符集），避免跨端不一致
  - 异常与兼容策略是否明确（旧算法迁移、失败降级、错误可观测）
  - 性能与安全权衡是否可控（大对象处理、流式、是否误用弱算法）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-加解密.md`

- **WebSocket**：
  - 连接管理是否集中（单例/多实例策略、页面生命周期绑定、资源释放）
  - 心跳、重连、退避与网络切换策略是否可控（前后台、弱网、断网恢复）
  - 消息协议是否版本化与可验证（Schema、ack/seq、幂等与乱序处理）
  - 安全与鉴权是否明确（token 刷新、过期处理、订阅权限边界）
  - 可观测性是否完善（连接状态、收发量、失败原因、延迟与告警）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-WebSocket.md`

- **日期时间与时区（DateTime / Timezone）**：
  - 日期解析与格式化是否统一入口（避免各处硬编码 `yyyy-MM-dd HH:mm:ss`、`dd/MM/yyyy`、ISO 模板）
  - 时区与 Locale 策略是否清晰（展示使用本地时区、服务端交互使用约定时区），并避免隐式依赖系统默认值
  - 时间戳单位是否显式治理（`timestampMs` / `timestampSec`），避免秒/毫秒混用导致比较与排序错误
  - 是否覆盖夏令时、跨时区、跨日边界（00:00 附近）等关键边界场景，避免日期偏移与展示错误
  - 国家/地区日期展示规则是否配置化，避免按国家码散落条件分支造成维护成本上升
  - **审查报告**：`/.plans/架构质量审查-003-Utils-DateTime.md`

- **Currency**：
  - 货币金额的表示是否统一（整数最小单位 vs Decimal），避免浮点误差
  - 格式化与舍入规则是否集中治理（round/floor/ceil、银行家舍入、展示精度）
  - 多币种与汇率来源是否明确（精度、时效、缓存、失败兜底）
  - i18n 与本地化展示是否一致（符号位置、千分位、小数点、负数）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-Currency.md`

- **Icon**：
  - 图标资源的组织与命名是否规范（命名空间、分模块、去重）
  - 加载策略是否合理（按需加载、tree-shaking、sprite/inline 选择）
  - 主题与多态支持是否清晰（颜色/尺寸/暗色模式、状态态图）
  - 性能与包体影响是否可控（重复资源、无用图标清理、缓存）
  - 可访问性是否考虑（aria-label、对比度、可替代文本策略）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-Icon.md`

- **设备信息（Device Info）**：
  - 设备信息采集是否最小化（仅采集业务必需字段），避免过度采集硬件标识与可识别信息
  - 字段来源与口径是否统一（系统 API、SDK 回传、自定义拼装），避免同一指标多源漂移
  - 设备指纹相关逻辑是否具备合规约束（告知/同意/撤回后降级），并避免将敏感标识明文落盘
  - 采集失败、权限受限、系统版本差异场景是否有兜底与降级策略
  - 是否建立可观测性（采集成功率、字段缺失率、上报延迟、异常告警）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-设备信息.md`

- **报表**：
  - 指标口径与数据源是否明确（字段定义、过滤条件、时间范围、维度）
  - 查询与聚合性能是否可控（分页、缓存、异步、限流、导出策略）
  - 展示与导出格式是否统一（日期/货币/百分比格式、排序口径、精度）
  - 权限与审计是否完善（数据权限、导出审批/水印、操作日志）
  - 异常与边界处理是否明确（空数据、超时、部分失败、重试与提示）
  - **审查报告**：`/.plans/架构质量审查-003-Utils-报表.md`

#### 3.3 其它关注点

- **职责单一性**：
  - 单个工具类/方法是否聚焦单一领域（如日期、字符串、文件、网络）
  - 是否存在"大杂烩式"工具类
  - 复用逻辑单元（如 mixins/composables/hooks/HOC 等）是否职责单一、可复用
  - 是否存在“工具层夹带业务规则/数据结构”的情况，导致工具层难以复用与迁移

- **命名与组织**：
  - 文件结构与命名是否清晰反映用途
  - 是否存在大量"CommonUtils"、"Utils2"等模糊命名
  - 复用逻辑单元命名是否清晰描述功能域
  - 工具函数是否存在重复实现（格式化/解析/下载/加密/日志等能力散落在页面/组件）

- **静态方法与全局状态**：
  - 是否过度使用全局状态，导致难以管理
  - 是否正确使用当前项目的状态管理方案管理全局状态
  - 是否避免在工具函数中直接操作 DOM

- **响应式使用**：
  - 工具层是否不依赖具体 UI 运行时/组件实例（除非通过明确的适配层注入）
  - 是否避免长期持有组件引用或订阅句柄，导致内存泄漏
  - 回调与上下文绑定是否可控（事件/定时器/订阅的注册与释放是否成对）

- **平台依赖与资源句柄**：
  - 对系统上下文/权限/文件句柄/网络连接等依赖是否通过明确边界注入与治理，避免隐式持有与跨层泄漏
  - 资源释放是否有统一约束（超时、取消、close/dispose），并可观测

- **可测试性**：
  - 工具方法是否易于通过单元测试验证
  - 是否与 UI 或外部环境强耦合导致无法隔离测试
  - 复用逻辑单元（如 mixins/composables/hooks/HOC 等）是否易于测试
  - 关键工具函数是否覆盖边界条件（空值/异常输入/编码/时区等）并具备回归保障

#### 3.4 审查产物输出确认

- **核心关注点审查报告**：
  - `/.plans/架构质量审查-003-Utils-Storage.md`
  - `/.plans/架构质量审查-003-Utils-Http.md`
  - `/.plans/架构质量审查-003-Utils-Log.md`
  - `/.plans/架构质量审查-003-Utils-加解密.md`
  - `/.plans/架构质量审查-003-Utils-WebSocket.md`
  - `/.plans/架构质量审查-003-Utils-DateTime.md`
  - `/.plans/架构质量审查-003-Utils-Currency.md`
  - `/.plans/架构质量审查-003-Utils-Icon.md`
  - `/.plans/架构质量审查-003-Utils-设备信息.md`
  - `/.plans/架构质量审查-003-Utils-报表.md`

- **整体审查报告（必须覆盖全部内容）**：
  - 必须覆盖本 Phase 全量核心关注点 + 其它关注点
  - 不能仅做条目汇总；必须给出跨主题的全局整合视角（职责边界、依赖方向、复用策略、风险优先级、治理路线图）
  - 整体审查报告需在收敛阶段补齐跨主题整合结论。
  - **整体审查报告**：`/.plans/架构质量审查-003-Utils.md`

### Phase 4 业务组件（业务功能层）

#### 4.1 审查目标

- 确保 UI 组件/页面结构清晰、职责明确，避免臃肿和过多业务逻辑堆积。
- 实现视图与业务逻辑的合理分离（如通过复用逻辑单元、状态管理、service 层等），提升可维护性与扩展性。
- 在业务复杂场景下保证组件状态管理、路由导航与数据交互的可控与可测试。

#### 4.2 核心关注点

- **首页**：
  - 首屏与冷启动体验是否可控（骨架屏/占位、首屏数据并行、懒加载策略）
  - 模块化与可配置性是否清晰（楼层/运营位、动态配置来源与校验）
  - **审查报告**：`/.plans/架构质量审查-004-业务页面-首页.md`

- **主旅程**：
  - 关键路径是否可追溯（入口、关键节点、失败回退、恢复路径）
  - 状态与副作用是否可控（跨页面共享状态、缓存策略、幂等与重入）
  - **审查报告**：`/.plans/架构质量审查-004-业务页面-主旅程.md`

- **营销位**：
  - 配置化与风控边界是否清晰（来源可信、字段校验、降级与兜底）
  - 展示与跳转协议是否统一（schema、路由参数、埋点与归因）
  - **审查报告**：`/.plans/架构质量审查-004-业务页面-营销位.md`

- **下单旅程**：
  - 表单/确认/支付等环节的状态机是否清晰（步骤、校验、回退、重试）
  - 并发与一致性风险是否评估（重复提交、接口幂等、价格/库存刷新）
  - **审查报告**：`/.plans/架构质量审查-004-业务页面-下单旅程.md`

- **品牌**：
  - 内容与资源治理是否可控（图片/视频/富文本来源、缓存、失败兜底）
  - SEO/分享/落地页协议（若适用）是否统一（title/meta、分享卡片、渠道参数）
  - **审查报告**：`/.plans/架构质量审查-004-业务页面-品牌.md`

- **用户协议**：
  - 合规链路是否闭环（展示版本、同意记录、撤回/更新提示）
  - 跳转与拦截策略是否安全（未同意的默认策略、关键流程前置拦截）
  - **审查报告**：`/.plans/架构质量审查-004-业务页面-用户协议.md`

#### 4.3 其它关注点

- **组件复杂度**：
  - 单个组件/页面文件行数、方法数是否过多
  - 是否存在组件规模长期失控的情况（例如约定阈值：单文件超过一定行数需要拆分）
  - 是否集中处理了过多流程逻辑
  - 可否拆分为子组件或下沉到复用逻辑单元（如 hooks/composables/mixins 等）
  - Props / Events 定义是否完整清晰（类型、默认值、事件协议、是否存在隐式依赖）

- **状态管理**：
  - 组件状态（加载中/成功/失败/空数据等）是否有统一的表示与切换方式
  - 是否存在大量 if-else 分支控制 UI 状态
  - 是否正确使用当前项目的状态管理方案管理全局状态
  - 是否以所选框架推荐的方式定义本地状态与派生状态（避免把“计算/派生”状态反复落盘成源状态）

- **视图与业务逻辑分离**：
  - UI 层是否只负责视图展示与用户交互
  - 业务逻辑是否下沉到复用逻辑单元/状态层/服务层（如 hooks/composables/mixins、store、services 等）
  - 组件内逻辑是否按职责拆分（事件处理、状态更新、数据转换、API 调用等）

- **路由与导航**：
  - 页面跳转是否集中管理或通过路由框架
  - 是否存在大量硬编码的路由路径字符串
  - 是否正确使用当前项目路由方案的导航守卫/拦截器机制（若存在）
  - 业务旅程是否可追溯（核心链路、列表-详情-审批流的路由地图是否可维护/可视化）
  - 动态路由（菜单下发/本地缓存拼装）是否具备统计与治理手段（路由注册日志/审计输出）
  - 深链/Schema（若存在）的协议、参数校验、来源可信与回退路径是否标准化并可审计

- **列表与复杂 UI**：
  - 表格、树形等复杂组件的设计是否清晰
  - 是否正确使用当前项目的 UI 组件库/设计系统（若存在）
  - 是否存在严重的布局嵌套或性能问题
  - 大数据量表格是否使用虚拟滚动
  - 列表页到详情页的跳转/缓存策略是否一致（hideInMenu/notCache 等约定是否统一）

- **异常与边界处理**：
  - 网络失败、权限拒绝、空数据、超时等场景是否有明确的用户反馈与回退策略
  - 是否正确处理错误边界
  - 是否提供友好的错误提示
  - 定时器、事件监听器、订阅等资源是否在组件卸载/销毁钩子中清理
  - 核心页面是否存在硬编码禁用/死代码，导致“页面看似存在但实际不可用”的风险

#### 4.4 审查产物输出确认

- **核心关注点审查报告**：
  - `/.plans/架构质量审查-004-业务页面-首页.md`
  - `/.plans/架构质量审查-004-业务页面-主旅程.md`
  - `/.plans/架构质量审查-004-业务页面-营销位.md`
  - `/.plans/架构质量审查-004-业务页面-下单旅程.md`
  - `/.plans/架构质量审查-004-业务页面-品牌.md`
  - `/.plans/架构质量审查-004-业务页面-用户协议.md`

- **整体审查报告（必须覆盖全部内容）**：
  - 必须覆盖本 Phase 全量核心关注点 + 其它关注点
  - 不能仅做条目汇总；必须给出跨主题的全局整合视角（组件拆分策略、状态边界、路由与导航治理、风险优先级、治理路线图）
  - 整体审查报告需在收敛阶段补齐跨主题整合结论。
  - **整体审查报告**：`/.plans/架构质量审查-004-业务页面.md`

### Phase 5 第三方SDK（集成与隔离）

#### 5.1 审查目标

- 通过适配层或封装层对第三方SDK进行隔离，降低对业务代码的侵入与耦合。
- 确保第三方SDK的使用安全、合规，可控地采集数据并满足隐私与合规要求。
- 便于后续替换或升级第三方SDK，降低供应商锁定风险。

#### 5.2 核心关注点

- **埋点/分析 SDK（行为分析、日志事件、崩溃与可观测性）**：
  - 技术栈边界是否清晰：平台分析 vs 自建数据平台 vs 第三方 SDK，是否避免重复采集
  - 事件/用户体系是否统一：命名、参数字典、用户 ID/属性、跨端口径
  - 数据质量是否可监控：丢失/延迟/重复、采样、动态开关、异常告警
  - 可观测性是否闭环：关键事件能否关联页面/请求/崩溃/核心业务动作
  - 性能与合规：初始化与上报是否异步可控，是否避免采集敏感信息
  - 事件协议与演进是否可治理（版本化、兼容、灰度开关、字段废弃策略）
  - **审查报告**：`/.plans/架构质量审查-005-SDK-埋点.md`

- **归因/结算 SDK（投放效果归因与转化回传）**：
  - 技术栈边界是否清晰：平台原生（Google/Meta/穿山甲）vs MMP（如 AppsFlyer/Adjust）vs 服务端回传
  - 转化回传是否可审计：触发时机、参数、失败重试、幂等/去重、异常告警
  - 口径是否统一：激活/注册/下单/放款等定义一致，避免多 SDK 口径漂移
  - 策略是否配置化：归因窗口/优先级/再归因策略，避免硬编码散落
  - 对账与合规：端/服/平台报表是否可对账，“结算口径以谁为准”是否明确；最小化采集与撤回同意降级
  - 回传可靠性与成本是否可控（失败重试、频控、异常告警、渠道隔离）
  - **审查报告**：`/.plans/架构质量审查-005-SDK-广告归因.md`

- **地图/定位 SDK（地理能力、POI、路线与地址解析）**：
  - 能力是否通过适配层提供：地图/定位/地理编码/POI/路线等避免业务直耦合
  - 权限与合规：权限分级、拒绝授权降级、采集频率/精度控制、避免未授权采集
  - 稳定性：服务不可达、Key 失效、SDK 异常时兜底不阻断主流程
  - 风控与成本：Key/签名/白名单、配额、异常调用监控、环境隔离
  - 性能：按需初始化，避免主线程阻塞与冷启动/首屏抖动
  - 地址与坐标体系是否统一（坐标系、精度、逆地理编码口径、缓存策略）
  - **审查报告**：`/.plans/架构质量审查-005-SDK-地图.md`

#### 5.3 其它关注点

- **封装与抽象**：
  - 是否为各类库（图表/地图/支付/认证等）提供统一封装与适配层，避免业务直调第三方 API
  - 依赖方向是否清晰：业务依赖自有抽象；SDK 细节被隔离并可替换

- **初始化与生命周期**：
  - 初始化是否集中管理（入口、依赖顺序、线程模型），是否避免阻塞冷启动/首屏
  - 生命周期是否正确：前后台切换、页面销毁、资源释放、重复初始化

- **错误与降级**：
  - SDK 不可用/调用失败/配置失效时是否有兜底与降级，且不阻断关键流程
  - 用户可感知的失败是否有可理解的提示与可恢复路径

- **版本管理**：
  - 版本是否可追踪、可复现（锁定/变更记录），是否关注弃用与安全公告
  - 是否具备升级策略与回滚预案（灰度、开关、兼容层）
  - 是否存在“引入但未使用/痕迹弱”的重型依赖（可裁剪）

- **权限与隐私**：
  - 权限申请是否最小化且可解释；拒绝授权是否可降级
  - 采集与上报是否与隐私政策一致；是否支持撤回同意后的停采/降级
  - 供应链风险：外部资源、Key/证书/签名、白名单与环境隔离是否规范

- **多库共存**：
  - 多个 UI 库、工具库共存时是否有统一的调度机制
  - 是否评估它们之间的冲突与性能影响
  - 是否正确处理样式冲突

#### 5.4 审查产物输出确认

- **核心关注点审查报告**：
  - `/.plans/架构质量审查-005-SDK-埋点.md`
  - `/.plans/架构质量审查-005-SDK-广告归因.md`
  - `/.plans/架构质量审查-005-SDK-地图.md`

- **整体审查报告（必须覆盖全部内容）**：
  - 必须覆盖本 Phase 全量核心关注点 + 其它关注点
  - 不能仅做条目汇总；必须给出跨主题的全局整合视角（隔离策略、供应链风险、合规边界、风险优先级、治理路线图）
  - 整体审查报告需在收敛阶段补齐跨主题整合结论。
  - **整体审查报告**：`/.plans/架构质量审查-005-SDK.md`

## 架构审查报告模板

待补充。

## 常见技术选型参考（References）

- Vue 3 + UniApp：见 `references/选型-vue3+uniapp.md`
- Vue 3 + Element Plus：见 `references/常用选型-vue3+elementplus.md`
- Vue 2 + iView：见 `references/常用选型-vue2-iview.md`
- Vue 2 + Vux + Webpack 3：见 `references/常用选型-vue2-vux-webpack3.md`
- Android：见 `references/常用选型-android.md`
- iOS：见 `references/常用选型-ios.md`
